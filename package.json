{
  "name": "cnap",
  "private": true,
  "scripts": {
    "dev": "concurrently -c \"blue.bold,green.bold\" -n \"CLIENT,SERVER\" \"pnpm dev:client\" \"pnpm dev:server\"",
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "test:server": "pnpm --filter server test",
    "build": "pnpm -r build",
    "build:client": "pnpm --filter client build",
    "build:server": "pnpm --filter server build",
    "type-check": "pnpm -r type-check",
    "biome": "pnpm -r biome",
    "start": "concurrently \"pnpm start:client\" \"pnpm start:server\"",
    "start:client": "pnpm --filter client start",
    "start:server": "pnpm --filter server start",
    "cleanup": "pnpm -r exec rimraf dist && rimraf node_modules pnpm-lock.yaml",
    "prepare": "husky"
  },
  "dependencies": {
    "concurrently": "9.1.2",
    "dotenv": "16.4.7",
    "drizzle-orm": "^0.43.1",
    "pg": "^8.16.0",
    "rimraf": "6.0.1"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/pg": "^8.15.2",
    "drizzle-kit": "^0.31.1",
    "husky": "^9.1.7",
    "tsx": "4.19.3"
  }
}